language: ruby
rvm:
  - "1.9.2"
  - "1.9.3"
  - jruby-19mode # JRuby in 1.9 mode
  - rbx-19mode
before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 3
env:
  - RACK_ENV=ci
script: "DISPLAY=:99.0 xvfb-run -a bundle exec rake ci"
